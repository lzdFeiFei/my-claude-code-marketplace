# X to WeChat Agent

一键启动专门的subagent，将X文章转换为微信公众号文章 - **不占用主agent上下文**。

## 核心理念

这是一个**轻量级launcher**，本身只有~1KB，主要功能都在独立的subagent中执行：

```
┌─────────────────────┐
│   Main Agent        │  ← 你在这里交互
│   (轻量,响应快)      │
└──────────┬──────────┘
           │ 启动
           ↓
┌─────────────────────┐
│   Subagent          │  ← 真正的工作在这里
│   (独立上下文)        │
│   • 爬取文章         │
│   • 翻译内容         │
│   • 优化排版         │
│   • 生成HTML         │
└─────────────────────┘
```

**优势**：
- ✅ 主agent保持轻量和响应
- ✅ 可以同时处理其他任务
- ✅ Subagent隔离运行，互不干扰
- ✅ 完成后自动清理，不留痕迹

## 使用方法

### 基本用法

```
把这个X文章转成公众号：
https://x.com/username/article/1234567890
```

就这么简单！Launcher会自动：
1. 识别你的需求
2. 启动专门的subagent
3. Subagent自主完成全流程
4. 返回结果给你

### 执行过程

**你看到的**：
```
收到！我将启动专门的agent来处理X文章转公众号的完整流程：
📥 爬取文章
🌐 翻译为中文
📱 优化排版
📄 生成HTML

预计2-3分钟完成。

[Subagent开始工作...]

✅ 步骤 1/4 完成：获取X文章内容
✅ 步骤 2/4 完成：翻译为中文
✅ 步骤 3/4 完成：优化公众号排版
✅ 步骤 4/4 完成：生成HTML

🎉 X文章转公众号完成！

📂 生成文件位置：./x-pipeline-temp/
主要文件：wechat-final.html ⭐
```

**背后发生的**：
- Main agent: 只用了几百tokens识别需求和启动subagent
- Subagent: 在独立上下文中完成所有处理
- Main agent: 接收结果并展示给你

### 自定义选项

```
转换这个X技术文章，用轻松的翻译风格，极简排版：
https://x.com/...
```

Launcher会识别你的偏好并传递给subagent。

## 安装方法

### 前置依赖

需要以下4个skills（subagent会调用）：
1. **x-article-fetcher** - 爬取X文章
2. **en-to-zh-translator** - 英译中
3. **wechat-md-formatter** - 公众号排版
4. **md-to-wechat** - 生成HTML

### 安装

```bash
# 通过marketplace安装（推荐）
/plugin marketplace add lzdFeiFei/my-claude-code-marketplace
/plugin install x-to-wechat-agent x-article-fetcher en-to-zh-translator wechat-md-formatter md-to-wechat
```

## 工作流程

### Subagent自主执行的步骤

```
1. 爬取 (x-article-fetcher)
   ↓ original-en.md

2. 翻译 (en-to-zh-translator)
   ↓ translated-zh.md

3. 排版 (wechat-md-formatter)
   ↓ formatted-zh.md

4. 生成HTML (md-to-wechat)
   ↓ wechat-final.html ⭐
```

### 文件输出

```
./x-pipeline-temp/
├── original-en.md          # 原始英文
├── translated-zh.md        # 中文译文
├── formatted-zh.md         # 优化后的Markdown
└── wechat-final.html       # 最终HTML ⭐
```

## 与Pipeline Skill的区别

| 特性 | Pipeline Skill | Agent Launcher (本方案) |
|------|---------------|----------------------|
| 上下文占用 | ~50KB常驻主agent | ~1KB launcher，工作在subagent |
| 主agent状态 | 被占用，无法处理其他任务 | 保持空闲，可并行处理 |
| Token使用 | 全在主agent | 主agent极少，subagent独立 |
| 执行方式 | 顺序调用skills | Subagent自主调用 |
| 适用场景 | 轻量工作流 | 重量级、长时间任务 |

**结论**：Agent方案更适合这种多步骤、长时间的转换任务。

## 技术细节

### Launcher Skill（主agent加载）

- 大小：~1KB
- 功能：
  - 识别X链接 + 公众号意图
  - 构造subagent prompt
  - 调用Task tool启动subagent
  - 展示subagent结果

### Subagent（独立运行）

- 类型：general-purpose agent
- 上下文：完全隔离
- 能力：
  - 完整tool access
  - 可调用所有skills
  - 可读写文件
  - 自主多步执行

### 通信机制

```
Main Agent
    ↓ (Task tool)
  [启动命令 + 完整workflow指令]
    ↓
Subagent
    ↓ (独立执行)
  [完成所有步骤]
    ↓ (Task tool result)
  [返回摘要和文件路径]
    ↓
Main Agent
    ↓
显示给用户
```

## 常见问题

### Q1: Subagent会占用额外的API费用吗？

A: 会的，subagent是独立的Claude实例。但总token使用量与pipeline skill类似，只是分配方式不同：
- Pipeline: 全在主agent
- Agent: 大部分在subagent，主agent只用很少

### Q2: 可以同时运行多个subagent吗？

A: 可以！这是agent方案的优势：
```
主agent保持空闲
   ↓
Subagent 1: 处理文章A
Subagent 2: 处理文章B (并行)
Subagent 3: 处理文章C (并行)
```

### Q3: Subagent失败了怎么办？

A: Subagent会报告：
- 完成到哪一步
- 具体错误信息
- 已生成的中间文件路径

你可以检查中间文件，手动修复后继续。

### Q4: 为什么不直接手动启动subagent？

A: 可以的！但launcher提供：
- 自动识别意图
- 标准化的workflow prompt
- 更好的用户体验
- 统一的错误处理

当然，你也可以手动：
```
启动一个agent处理X文章转公众号：
[提供URL和详细指令]
```

### Q5: Launcher skill本身会一直占用上下文吗？

A: 是的，但只占~1KB。相比50KB的pipeline skill，这几乎可以忽略。而且launcher只在需要时触发，大部分时间是休眠的。

### Q6: Subagent能访问主agent的上下文吗？

A: 不能，也不需要。Subagent通过launcher传递的prompt获得所有必要指令，然后完全独立工作。

## 使用建议

### 最佳实践

1. **单任务执行**
   ```
   把这个X文章转成公众号：[URL]
   ```
   简单直接，让launcher和subagent自动处理一切。

2. **批量处理**
   ```
   把这3个X文章都转成公众号：
   1. [URL1]
   2. [URL2]
   3. [URL3]
   ```
   Main agent会为每个URL启动一个subagent，并行处理。

3. **自定义风格**
   ```
   转换这个文章，用正式语气，现代排版风格：[URL]
   ```
   Launcher会识别偏好并传递给subagent。

### 性能优化

- **并行处理**: 多个URL时自动并行
- **快速响应**: 主agent立即确认，不等待完成
- **资源隔离**: Subagent失败不影响主agent

## 故障排除

### 问题：Launcher没有启动subagent

**原因**: 可能没识别到intent

**解决**: 明确说明
```
使用 x-to-wechat-agent 处理：[URL]
```

### 问题：Subagent报告skill缺失

**原因**: 依赖的skills未安装

**解决**: 安装所需skills
```
/plugin install x-article-fetcher en-to-zh-translator wechat-md-formatter md-to-wechat
```

### 问题：Subagent执行很慢

**原因**: 正常现象，需要调用4个skills

**预期**: 2-3分钟完成

## 更新日志

### v1.0.0 (2026-01-14)
- ✨ 初始版本
- ✅ 轻量级launcher (~1KB)
- ✅ 完整subagent workflow
- ✅ 支持自定义风格
- ✅ 支持并行处理

## 许可证

MIT License

---

**开始使用**: 只需说 "把这个X文章转成公众号：[URL]"，其他的交给subagent！